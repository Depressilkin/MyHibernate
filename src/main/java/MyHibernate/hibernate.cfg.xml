<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-configuration SYSTEM
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- URL, пользователь, пароль -->
        <property name="connection.driver_class">org.postgresql.Driver</property>
        <property name="connection.url">jdbc:postgresql://localhost:5432/users</property>
        <property name="connection.username">admin</property>
        <property name="connection.password">admin</property>

        <!-- Dialect для PostgreSQL -->
        <property name="dialect">org.hibernate.dialect.PostgreSQLDialect</property>

        <!-- Другие параметры -->
        <property name="hbm2ddl.auto">update</property> <!-- для автоматического обновления схемы -->
        <property name="show_sql">true</property> <!-- для отображения SQL в консоли -->

        <!-- Маппинг ваших сущностей -->
        <mapping class="com.example.YourEntity"/>
    </session-factory>
</hibernate-configuration>